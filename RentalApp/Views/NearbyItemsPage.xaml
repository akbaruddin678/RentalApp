<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="RentalApp.Views.NearbyItemsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto,Auto,Auto,*" Padding="15">

        <!-- Error -->
        <Border Grid.Row="0" BackgroundColor="#FFEBEE" Padding="10"
                IsVisible="{Binding HasError}" Margin="0,5">
            <Border.StrokeShape><RoundRectangle CornerRadius="8"/></Border.StrokeShape>
            <Label Text="{Binding ErrorMessage}" TextColor="#D32F2F" />
        </Border>

        <!-- Radius Slider -->
        <StackLayout Grid.Row="1" Margin="0,10">
            <Label Text="{Binding SearchRadius, StringFormat='Search Radius: {0:F1} km'}"
                   FontSize="14" FontAttributes="Bold" />
            <Slider Value="{Binding SearchRadius}" Minimum="1" Maximum="50"
                    ThumbColor="#2196F3" MinimumTrackColor="#2196F3" />
        </StackLayout>

        <!-- Search Button + Status -->
        <StackLayout Grid.Row="2" Spacing="10">
            <Button Text="🔍 Find Near Me"
                    Command="{Binding FindNearbyCommand}"
                    BackgroundColor="#2196F3" TextColor="White"
                    CornerRadius="8" HeightRequest="50"
                    FontSize="16" FontAttributes="Bold" />
            <Label Text="{Binding LocationStatus}"
                   FontSize="13" TextColor="Gray" HorizontalOptions="Center" />
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                               Color="#2196F3" />
        </StackLayout>

        <!-- Results -->
        <CollectionView Grid.Row="3" ItemsSource="{Binding NearbyItems}"
                        EmptyView="No items found nearby. Try increasing the radius.">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,5" Padding="15" BackgroundColor="White" StrokeThickness="1">
                        <Border.StrokeShape><RoundRectangle CornerRadius="10"/></Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToItemDetailCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding Title}" FontSize="16" FontAttributes="Bold" />
                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="{Binding DailyRate, StringFormat='£{0:F2}/day'}"
                                   FontSize="14" TextColor="#2196F3" FontAttributes="Bold" />
                            <Label Grid.Row="1" Grid.ColumnSpan="2"
                                   Text="{Binding Description}" FontSize="13"
                                   TextColor="Gray" MaxLines="2" />
                            <Label Grid.Row="2" Grid.Column="0"
                                   Text="{Binding LocationName, StringFormat='📍 {0}'}"
                                   FontSize="12" TextColor="Gray" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
